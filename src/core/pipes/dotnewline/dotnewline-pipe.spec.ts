import { DotnewlinePipe } from './dotnewline-pipe';

describe('DotnewlinePipe', () => {
  it('create an instance', () => {
    const pipe = new DotnewlinePipe();
    expect(pipe).toBeTruthy();
  });
});
